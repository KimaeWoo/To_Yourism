<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/tossBackground"
    android:padding="5dp">

    <!-- Spinner for selecting region -->
    <Spinner
        android:id="@+id/regionSpinner"
        android:layout_width="wrap_content"
        android:layout_height="40dp"
        android:layout_alignParentStart="true"
        android:layout_alignParentEnd="true"
        android:layout_marginStart="100dp"
        android:layout_marginEnd="100dp"
        android:layout_marginBottom="4dp"
        android:textColor="#000000"
        android:textSize="16sp" />

    <!-- RecyclerView for displaying tourist spots -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/touristSpotRecyclerView"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/regionSpinner"
        android:layout_marginStart="4dp"
        android:layout_marginTop="10dp"
        android:layout_marginEnd="4dp"
        android:background="@color/tossBackground"
        android:orientation="horizontal"
        android:scrollbars="horizontal" />

    <!-- LinearLayout for pagination controls -->
    <LinearLayout
        android:id="@+id/pageControlsLayout"
        android:layout_width="395dp"
        android:layout_height="40dp"
        android:layout_below="@id/touristSpotRecyclerView"
        android:layout_marginTop="20dp"
        android:layout_marginBottom="20dp"
        android:gravity="center"
        android:orientation="horizontal">

        <ImageButton
            android:id="@+id/btnPrevPage"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:layout_marginEnd="50dp"
            android:background="@android:color/transparent"
            android:padding="8dp"
            android:scaleType="centerInside"
            android:src="@drawable/left" />

        <TextView
            android:id="@+id/tvPageNumber"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="16dp"
            android:layout_marginEnd="16dp"
            android:gravity="center"
            android:text="Page 1"
            android:textColor="@color/tossTextPrimary"
            android:textSize="16sp" />

        <ImageButton
            android:id="@+id/btnNextPage"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:layout_marginStart="50dp"
            android:background="@android:color/transparent"
            android:padding="8dp"
            android:scaleType="centerInside"
            android:src="@drawable/right" />
    </LinearLayout>

    <!-- RecyclerView for displaying top posts -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/topPostRecyclerView"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/pageControlsLayout"
        android:layout_alignParentStart="true"
        android:layout_alignParentEnd="true"
        android:layout_alignParentBottom="true"
        android:layout_marginStart="4dp"
        android:layout_marginEnd="4dp"
        android:background="@color/tossBackground"
        android:orientation="horizontal"
        android:scrollbars="horizontal" />
</RelativeLayout>
